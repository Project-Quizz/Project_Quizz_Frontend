@model List<HighscoreDataDto>

@{
    var orderedMultiList = Model.OrderByDescending(x => x.TotalPointWorthMulti).ToList();
}

<table id="" class="display hover" style="width:100%">
    <caption>Highscore Tabelle Gesamt</caption>
    <thead>
        <tr>
            <th>Username</th>
            <th>Spiele</th>
            <th>Punktwert</th>
        </tr>
    </thead>
    <tbody>
        @foreach (var user in orderedMultiList)
        {
            <tr>
                <td>@user.UserId</td>
                <td>@user.TotalMultiGames</td>
                <td>@user.TotalPointWorthMulti</td>
            </tr>
        }
    </tbody>
</table>

